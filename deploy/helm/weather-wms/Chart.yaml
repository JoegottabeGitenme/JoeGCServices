apiVersion: v2
name: weather-wms
description: OGC WMS service for meteorological data
type: application
version: 0.1.0
appVersion: "0.1.0"

dependencies:
  - name: redis
    version: "18.x.x"
    repository: "https://charts.bitnami.com/bitnami"
    condition: redis.enabled
  - name: postgresql
    version: "13.x.x"
    repository: "https://charts.bitnami.com/bitnami"
    condition: postgresql.enabled
  - name: minio
    version: "12.x.x"
    repository: "https://charts.bitnami.com/bitnami"
    condition: minio.enabled

maintainers:
  - name: Your Name
    email: you@example.com
